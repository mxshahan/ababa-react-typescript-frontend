import React from 'react';

/**
 * Login Component
 * Validate Field -> email: isEmail, isEmpty
 * Validate Field -> password: isEmpty, minLength(6)
 */

export const Login: React.FC = () => {
  return <div>login</div>;
};

